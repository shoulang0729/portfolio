# Portfolio Heatmap — アプリ仕様書

> 最終更新: 2026-05-16 / バージョン: 20260516b

---

## 目次

**外部設計（ユーザーから見た仕様）**
1. [アプリ概要](#1-アプリ概要)
2. [共通UI仕様](#2-共通ui仕様)
3. [ヒートマップタブ](#3-ヒートマップタブ)
4. [銘柄リストタブ](#4-銘柄リストタブ)
5. [ウォッチリストタブ](#5-ウォッチリストタブ)
6. [資産推移タブ](#6-資産推移タブ)
7. [AI相談タブ](#7-ai相談タブ)
8. [データソースと更新仕様](#8-データソースと更新仕様)

**内部設計（実装情報）**
9. [システム構成](#9-システム構成)
10. [ファイル構成と依存関係](#10-ファイル構成と依存関係)
11. [データ取得の詳細フロー](#11-データ取得の詳細フロー)
12. [状態管理](#12-状態管理)
13. [データ永続化](#13-データ永続化)
14. [セキュリティ・認証の実装](#14-セキュリティ認証の実装)
15. [CSSテーマ設計](#15-cssテーマ設計)

**運用**
16. [デプロイ手順](#16-デプロイ手順)
17. [改善ロードマップ](#17-改善ロードマップ)
18. [変更履歴](#18-変更履歴)

---

# 外部設計（ユーザーから見た仕様）

---

## 1. アプリ概要

### 何をするアプリか

日米の保有株式・投資信託をリアルタイムで可視化・分析するプライベートツール。
個人投資家（保有資産 5 億円超）が自分の資産状況を素早く把握するために使う。

### アクセス情報

| 項目 | 内容 |
|------|------|
| **本番URL** | https://shoulang0729.github.io/portfolio/ |
| **ホスティング** | GitHub Pages（静的サイト） |
| **対応デバイス** | PC・スマートフォン（PWA対応。ホーム画面に追加可能） |
| **認証** | 起動時に4桁PINで保護（デフォルト: 1234） |

### タブ構成

| タブ | 概要 |
|------|------|
| **ヒートマップ** | 保有銘柄を時価評価額の大きさで並べた色付きマップ |
| **銘柄リスト** | 保有銘柄の詳細テーブル（ソート・期間別騰落率） |
| **ウォッチリスト** | 保有外の気になる銘柄を追加・監視 |
| **資産推移** | 日次の総資産額をグラフで記録・表示 |
| **AI相談** | 5つのAIに同時に投資相談できる |

---

## 2. 共通UI仕様

### ヘッダー（常時固定）

画面上部に常に表示される固定ヘッダー。スクロールしても消えない。

**左側**: アプリタイトル「Heatmap」＋バージョン番号

**右側（上段）**:
- **更新頻度ボタン**: OFF / ライブ / 1分 / 5分 / 60分 から選択
- **テーマボタン**: ☼（ライト）/ ☾（ダーク）/ A（システム自動）を循環
- **PINボタン**: PINコードの変更

**右側（下段）**:
- **カウントダウン**: 次回自動更新までの残り時間
- **ステータス**: データ取得状況（黄: 取得中 / 緑: 完了 / 赤: 失敗）

### 統計バー

ヘッダー直下に表示。右端の目のアイコンで表示/非表示を切替。

| 表示項目 | 内容 |
|---------|------|
| 資産総額 | 全銘柄の時価評価額の合計（円） |
| 含み損益 | 取得原価との差額（円・%） |
| 期間別損益 | 1日・1週・1ヶ月…10年の各期間の損益額と騰落率 |

> 期間別損益は「今の保有銘柄を過去にも持っていたら」という**シミュレーション値**であり、実際の運用損益ではない。

### テーマ

| モード | 動作 |
|-------|------|
| ライト | 常に白背景 |
| ダーク | 常に黒背景 |
| 自動（A） | OSのダーク/ライト設定に自動追従 |

設定はブラウザに記憶される（ページを閉じても保持）。

---

## 3. ヒートマップタブ

### 画面構成

保有銘柄を**時価評価額に比例したマスの大きさ**でレイアウトし、**騰落率または含み損益率**で色付けする。

**2グループに分かれて表示**:
- 米国株・ETF（上）
- 日本株・ETF・投資信託（下）

### 色の意味（日本株の慣習に準拠）

| 色 | 意味 |
|---|------|
| 濃い赤 | 大きく上昇 |
| 薄い赤 | 小幅上昇 |
| グレー | ほぼ変動なし |
| 薄い緑 | 小幅下落 |
| 濃い緑 | 大きく下落 |

### カラーモードの切替

ヘッダー下の期間ボタンで切替：

| 操作 | 表示内容 |
|------|---------|
| 「1日」「1週」…「10年」ボタンをクリック | その期間の騰落率で色付け |
| 「含み損益」ボタンをクリック | 購入時からの含み損益率で色付け |
| 「含み損益」を再クリック | 騰落率表示に戻る |

### セルの情報

| セルサイズ | 表示内容 |
|-----------|---------|
| 大・中セル | ティッカーシンボル ＋ 騰落率（%） |
| 小セル | ティッカーシンボルのみ |
| 極小セル | 何も表示しない |

### ツールチップ（セルにカーソルを乗せると表示）

- 銘柄名・シンボル
- 現在値・平均取得単価・保有数
- 時価評価額・含み損益（円・%）
- 前日比（円・%）
- 投資信託の場合は「📊 騰落率は代替インデックスで近似」の注記

### チャートを開く

セルをクリックすると、その銘柄の株価チャートがモーダルで開く。

### 価格更新時のアニメーション

自動更新または手動更新で価格が変化したとき、該当セルが一瞬光る（上昇：明るく / 下落：暗く）。

---

## 4. 銘柄リストタブ

### テーブル列

| 列名 | 内容 | 備考 |
|------|------|------|
| ティッカー/銘柄名 | シンボルと銘柄名 | 横スクロール時も左端に固定 |
| 市場 | 東証 / US / 投信 | |
| 現在値 | 現在の株価 | |
| 1d〜10y | 各期間の騰落率（色付き） | |
| 含み損益 | 損益額（円） | |
| 損益率 | 含み損益率（%、色付き） | |
| 時価評価額 | （詳細列） | 目のアイコンで表示切替 |
| 保有数 | （詳細列） | 目のアイコンで表示切替 |
| 取得単価 | （詳細列） | 目のアイコンで表示切替 |

### バーグラフ

各行の背景に時価評価額の比率を示す横棒が表示される（最大保有銘柄を100%として比較）。

### ソート

列ヘッダーをクリックするとその列で並び替え。再クリックで昇順/降順が切替わる。

---

## 5. ウォッチリストタブ

### できること

- **銘柄の追加**: 上部の検索欄にティッカーまたは銘柄名を入力して検索・追加
- **価格監視**: 追加した銘柄の現在値・騰落率をリアルタイムで確認
- **ソート**: 銘柄リストと同様に列ヘッダーでソート
- **削除**: 行右端の「✕」ボタンで削除

### 検索の動作

| 入力例 | 自動判定 |
|-------|---------|
| `AAPL` | 米国株として検索 |
| `7203` | 日本株（7203.T）として検索 |
| `9988` | 香港株（9988.HK）もあわせて検索 |

検索結果にはティッカー・銘柄名・市場・種別（株/ETF/投信/通貨）が表示される。

### 保存

追加した銘柄はブラウザに記憶される（ページを閉じても保持）。

---

## 6. 資産推移タブ

### できること

- **自動記録**: 価格更新が完了するたびに、その日の総資産額を自動で記録する
- **グラフ表示**: 記録された日次総資産額をD3折れ線グラフで表示
- **CSV出力**: 記録データをCSVファイルとしてダウンロード

### 記録の仕様

- 1日1レコード（同日に複数回更新しても最新値で上書き）
- 最大1,000日分を保存（約3年分）
- ブラウザのローカルストレージに保存

---

## 7. AI相談タブ

### できること

投資に関する質問を入力すると、5つのAIが同時に回答し、最終的にClaudeが統括する。

### AIモデル構成

| AI | モデル | 役割 |
|----|-------|------|
| ChatGPT | GPT-4o | 独立回答 |
| Gemini | gemini-2.0-flash | 独立回答 |
| Grok | grok-3-latest | 独立回答 |
| DeepSeek | deepseek-chat | 独立回答 |
| **Claude** | claude-sonnet-4-6 | 他4モデルの回答を踏まえて**統括** |

### 質問の流れ

1. 質問を入力する
2. 「保有銘柄情報を含める」にチェックを入れると、ポートフォリオ情報（銘柄・評価額・損益率）がAIに渡される
3. 「送信」をクリック
4. ChatGPT / Gemini / Grok / DeepSeek が同時に回答を開始する
5. 4モデルの回答が出揃ったあと、Claudeが他モデルの回答を参考にしながら最終的な統括回答を出す

### 会話の継続

同じセッション内で会話履歴が保持され、文脈を踏まえた対話ができる。

### APIキーの設定

初回利用時に各AIのAPIキーを設定する必要がある。
キーはCloudflare Workerのサーバー側で管理されるため、ブラウザ上には保存されない。

---

## 8. データソースと更新仕様

### データの取得元

| データ種別 | 主なソース | 補足 |
|-----------|-----------|------|
| ライブ株価・当日騰落率 | Finnhub API | 失敗時はYahoo Financeへ自動切替 |
| 株価履歴（1日〜10年） | Yahoo Finance API | Finnhubは精度不足のため使わない |
| AI回答 | 各AIのAPI | Cloudflare Worker経由でAPIキーを保護 |

> すべてのAPI通信はバックエンドプロキシ（Cloudflare Worker）を経由する。
> これによりAPIキーがブラウザ上に露出しない。

### 自動更新

| 設定 | 動作 |
|------|------|
| OFF | 自動更新なし |
| ライブ | 即時1回取得後、1分間隔に移行 |
| 1分 / 5分 / 60分 | 指定間隔で自動更新 |

バックグラウンド（タブ非表示）中はカウントダウンが停止し、タブに戻ると再開する。

### 為替レート

現状、USD/JPYのリアルタイム為替は取得していない。
米国株の円建て評価額は「前回価格からの価格変動比率」で更新されるため、為替変動は反映されない（改善計画に記載）。

### 投資信託の扱い

Finnhub・Yahoo Financeに未収録の投資信託（ひふみ等）は、対応する代替インデックス（例：eMaxis Slim全世界株式）の騰落率で近似する。ツールチップに「📊 騰落率は代替インデックスで近似」と表示される。

---

# 内部設計（実装情報）

---

## 9. システム構成

```
ブラウザ（GitHub Pages）
    │
    ├── 株価・AI取得 ──→ Cloudflare Worker（portfolio-proxy.shoulang.workers.dev）
    │                        ├── /yahoo   → Yahoo Finance API
    │                        ├── /finnhub → Finnhub API（APIキーはWorker内で管理）
    │                        └── /ai/*    → 各AI API（APIキーはWorker内で管理）
    │
    └── ローカル保存
             ├── localStorage   → テーマ・ウォッチリスト・PIN・資産履歴
             └── sessionStorage → 認証トークン・AES鍵・履歴価格キャッシュ
```

### Cloudflare Worker エンドポイント一覧

| エンドポイント | メソッド | 用途 |
|--------------|---------|------|
| `/yahoo?url=<encoded>` | GET | Yahoo Finance プロキシ |
| `/finnhub?path=<path>&<params>` | GET | Finnhub プロキシ |
| `/ai/openai` | POST | OpenAI プロキシ |
| `/ai/gemini` | POST | Gemini プロキシ |
| `/ai/grok` | POST | Grok プロキシ |
| `/ai/deepseek` | POST | DeepSeek プロキシ |
| `/ai/claude` | POST | Anthropic プロキシ |

Worker の環境変数（Cloudflare Secrets）:
`FINNHUB_API_KEY` / `OPENAI_API_KEY` / `GEMINI_API_KEY` / `GROK_API_KEY` / `DEEPSEEK_API_KEY` / `ANTHROPIC_API_KEY`

---

## 10. ファイル構成と依存関係

### フロントエンド（GitHub Pages）

| ファイル | 役割 |
|---------|------|
| `index.html` | HTML骨格・スクリプトロード順制御 |
| `auth.js` | PIN認証・AES-GCM暗号化鍵の導出・PIN変更 |
| `positions.js` | 保有銘柄データ・期間設定（PERIODS） |
| `state.js` | グローバル定数(C)・アプリ状態(state) |
| `utils.js` | フォーマッター・色計算・テーブルヘルパー |
| `data.js` | API通信・価格取得・履歴キャッシュ |
| `heatmap.js` | D3.js ツリーマップ描画 |
| `chart.js` | D3.js 個別銘柄チャート（モーダル） |
| `stock-list.js` | 銘柄リストタブ（テーブル・ソート・バーグラフ） |
| `watchlist.js` | ウォッチリストタブ |
| `history.js` | 資産推移タブ（日次記録・グラフ） |
| `ai-tab.js` | AI相談タブ（5モデル並列・会話継続） |
| `app.js` | 初期化・タブ切替・テーマ・自動更新 |
| `portfolio.css` | 全スタイル（テーマ変数・レスポンシブ） |

### バックエンド（Cloudflare Workers）

| ファイル | 役割 |
|---------|------|
| `worker/src/index.js` | Worker本体（ルーティング・プロキシ処理） |
| `worker/wrangler.toml` | Cloudflare設定（Worker名・環境変数） |

### スクリプトロード順

```
auth.js → positions.js → state.js → utils.js → data.js
→ heatmap.js → chart.js → stock-list.js → watchlist.js
→ history.js → ai-tab.js → app.js
```

> **現状の課題**: すべての関数がグローバルスコープに露出。ES Modules 化が改善計画に含まれる。

### 依存関係

```
positions.js ──────────────────────────────────────────────┐
state.js ───────────────────────────────────────────────┐  │
utils.js (← state, positions, D3) ───────────────────┐  │  │
data.js  (← state, utils, positions) ─────────────┐  │  │  │
auth.js  （独立・暗号化鍵のみ他から参照）            │  │  │  │
                                                     │  │  │  │
heatmap.js    (← state, utils, positions, D3) ───────┤  │  │  │
chart.js      (← state, utils, data, D3) ────────────┤  │  │  │
stock-list.js (← state, utils, positions) ───────────┤  │  │  │
watchlist.js  (← state, utils, data, positions) ─────┤  │  │  │
history.js    (← state, utils, positions) ───────────┤  │  │  │
ai-tab.js     (← auth, positions) ───────────────────┤  │  │  │
app.js        (← 全モジュール) ──────────────────────┘──┘──┘──┘
```

---

## 11. データ取得の詳細フロー

### ライブ価格取得（`fetchLivePrice` in data.js）

```
fetchLivePrice(ySymbol)
  │
  ├─① Finnhub（Worker経由: /finnhub?path=/quote&symbol=TYO:9983）
  │    返値: { price: d.c, dayPct: d.dp }
  │    スキップ条件: d.c === 0（週末・未収録銘柄）
  │
  └─② Yahoo Finance（失敗時フォールバック）
       Worker経由: /yahoo?url=<encoded Yahoo Finance URL>
       さらに失敗時: corsproxy.io → allorigins の順に試行
```

### 履歴データ取得（`fetchAllHistorical` in data.js）

Yahoo Finance のみ使用（Finnhubはスプリット未調整のため不使用）。

```
fetchAllHistorical(range)  range = '1y' | '5y' | '10y'
  ├─ 重複防止: fetchingRanges に range を追加（終了時に削除）
  ├─ キャッシュ済みシンボルをスキップ
  ├─ 5銘柄ずつバッチ取得（300msインターバル）
  └─ 失敗銘柄を2秒後にリトライ
```

### 全銘柄一括更新（`refreshPrices` in data.js）

```
refreshPrices()
  ├─ 5銘柄バッチで fetchLivePrice を並列実行
  ├─ 失敗銘柄を2秒後リトライ
  └─ 価格更新後の計算
       JPY建て: value = price × shares
       USD建て: 前回価格比率で円建て value をスケール
```

### 異常価格スキップ

| 場所 | 条件 | 処理 |
|------|------|------|
| `refreshPrices` | live.price / 前回価格 が 0.1未満 or 10超 | スキップ（前回値を保持） |
| 履歴キャッシュ更新 | price / 直近履歴値 が 0.3未満 or 3.0超 | スキップ |

### シンボル変換（Yahoo Finance → Finnhub）

| Yahoo Finance | Finnhub |
|--------------|---------|
| `9983.T` | `TYO:9983` |
| `AAPL` | `AAPL` |

### スプリット自動補正（`applySplitCorrection`）

1日で±50%超の価格変動を株式分割と判定し、過去データを遡って自動補正する。

---

## 12. 状態管理

アプリの状態は `state.js` のグローバル `state` オブジェクト一本で管理する（Redux等は不使用）。

### 主要なstateフィールド

| フィールド | 説明 |
|-----------|------|
| `colorMode` | ヒートマップの色モード（`'change'` or `'pnl'`） |
| `changePeriod` | 選択中の期間ID（`'1d'`〜`'10y'`） |
| `historicalCache` | 履歴データキャッシュ（`{range: {symbol: [{date, close}]}}`） |
| `fetchingRanges` | 取得中のレンジSet（重複リクエスト防止） |
| `yahooCrumb` / `yahooCrumbExpiry` | Yahoo Finance 認証トークンと有効期限（55分） |
| `autoSec` / `countdownVal` | 自動更新間隔と残り秒数 |
| `themeMode` | テーマモード（`'auto'` / `'light'` / `'dark'`） |
| `listSortCol` / `listSortDir` | 銘柄リストのソート状態 |
| `wlSortCol` / `wlSortDir` | ウォッチリストのソート状態 |
| `activeTab` | 現在のタブ名 |
| `watchlist` | ウォッチリスト銘柄配列 |
| `prevPrices` | 価格更新アニメーション用の前回価格キャッシュ |

---

## 13. データ永続化

### localStorage（ブラウザを閉じても保持）

| キー | 内容 | 担当 |
|------|------|------|
| `hm-theme` | テーマモード | app.js |
| `hm-watchlist` | ウォッチリスト銘柄（JSON配列） | watchlist.js |
| `hm-pin-hash` | 変更後のPINのSHA-256ハッシュ | auth.js |
| `hm-asset-history` | 日次総資産額レコード（JSON配列、最大1,000件） | history.js |

### sessionStorage（タブを閉じると消える）

| キー | 内容 | 担当 |
|------|------|------|
| `hm-auth-v1` | 認証済みフラグ | auth.js |
| `hm-enc-key-v1` | AES-256-GCM鍵（Base64） | auth.js |
| `hm-hist-cache` | 履歴価格キャッシュ（リロード時に復元、APIリクエスト節約） | data.js |

---

## 14. セキュリティ・認証の実装

### PIN認証フロー（`auth.js`）

```
起動
  └─ sessionStorage['hm-auth-v1'] を確認
       ├─ あり → 認証済み、アプリ表示
       └─ なし → PINキーパッドを全画面表示
                   └─ 4桁入力 → SHA-256ハッシュで照合
                                 ├─ 一致 → AES-256-GCM鍵をPBKDF2導出（10万回反復）
                                 │         → sessionStorageに鍵・認証フラグ保存
                                 └─ 不一致 → 5回失敗で30秒ロック
```

### Cloudflare WorkerによるAPIキー保護

AIのAPIキーおよびFinnhub APIキーはCloudflare WorkerのSecrets（環境変数）に保存する。
ブラウザのJavaScriptコードにAPIキーは一切含まれない。

---

## 15. CSSテーマ設計

テーマは `html[data-theme]` 属性で制御。`applyTheme()` が `auto` を matchMedia で解決して常に `"light"` または `"dark"` を明示セットする。

### 主要CSS変数

| 変数 | ライト | ダーク |
|------|-------|-------|
| `--bg` | #F2F2F7 | #000000 |
| `--surface` | #FFFFFF | #1C1C1E |
| `--text` | #1C1C1E | #FFFFFF |
| `--text2` | #8E8E93 | #8E8E93 |
| `--border` | #C6C6CB | #3A3A3C |
| `--null-cell` | #E5E5EA | #2C2C2E |

### ヒートマップの色スケール

| 期間 | scale値 | 意味 |
|------|---------|------|
| 1日 | 4 | ±4%で最大色 |
| 1週 | 8 | ±8%で最大色 |
| 1ヶ月 | 15 | ±15%で最大色 |
| 1年 | 65 | ±65%で最大色 |
| 含み損益 | 50 | ±50%で最大色 |

---

# 運用

---

## 16. デプロイ手順

### フロントエンド（GitHub Pages）

```bash
# 1. index.html の全 ?v= パラメータを更新
#    形式: ?v=YYYYMMDDX（例: 20260516a, 20260516b, ...）
#    同日複数リリース時は末尾アルファベットを a, b, c... と進める

# 2. コミット＆Push（VSCode Source Control または git コマンド）
git add -A
git commit -m "deploy: vYYYYMMDDX"
git push origin main

# 注意: push.sh はGitHubトークンを含むため .gitignore 登録済み。
#       絶対にコミットしないこと。
```

### バックエンド（Cloudflare Worker）

```bash
cd worker
./node_modules/.bin/wrangler deploy
```

APIキーの追加・変更のみの場合は `wrangler secret put <KEY_NAME>` で対応（再デプロイ不要）。

---

## 17. 改善ロードマップ

### 完了済み

| 機能 | バージョン |
|------|----------|
| sessionStorageによる履歴キャッシュ永続化 | 20260516a |
| 資産推移タブ（history.js）の新規実装 | 20260516a |
| AI相談の会話継続化・Claudeストリーミング | 20260516a |
| 価格更新時のセルフラッシュアニメーション | 20260516a |
| 銘柄リストのティッカー列sticky固定 | 20260516a |
| 初回データ取得中のスケルトン表示 | 20260516a |
| システムテーマの自動追従改善 | 20260516a |
| Cloudflare WorkerによるAPIプロキシ実装 | 20260516b |

### 着手予定

| # | 機能 | 概要 |
|---|------|------|
| 1 | **ai-tab.js のWorker対応** | AI APIキーをWorker経由に完全移行し、ブラウザ側のキー管理を廃止 |
| 2 | **USD/JPY為替レートのリアルタイム取得** | Yahoo Finance `/USDJPY=X` で取得し、米国株の円換算を正確化 |
| 3 | **リバランス提案パネル** | 目標比率と現在比率の差分表示 |

### 検討中

- ES Modules 化（グローバルスコープからの脱却）
- 資産贈与・税務シミュレーター

---

## 18. 変更履歴

| バージョン | 日付 | 内容 |
|-----------|------|------|
| 20260516b | 2026-05-16 | Cloudflare Worker プロキシ実装（Yahoo Finance・Finnhub・AI API）、APIキーをWorker Secretsに移行 |
| 20260516a | 2026-05-16 | sessionStorageキャッシュ・資産推移タブ・AI会話継続化・フラッシュアニメーション・ティッカー列固定・スケルトン表示・テーマ自動追従改善 |
| 20260514a | 2026-05-14 | ポートフォリオ更新（1306追加・JPST追加・GDX/SHV削除・株数・価格更新） |
| 20260417j | 2026-04-17 | ポートフォリオ更新（1629分割・8050分割・PLTR追加）・Finnhub異常価格スキップ修正 |
| 20260322f | 2026-03-22 | PWAアイコン実装（SVG favicon・PNG各サイズ・manifest.json） |
| 20260322e | 2026-03-22 | 投資信託追加（ひふみ3銘柄・PIMCO-ST） |
| 20260322b | 2026-03-22 | ウォッチリストのスマホ横スクロール修正 |
| 20260322a | 2026-03-22 | Finnhub実装（優先→Yahoo フォールバック）・ポートフォリオ更新 |
| 20260311o | 2026-03-11 | Yahoo Finance安定性改善（query2追加・バッチ取得・リトライ） |
| 20260311n | 2026-03-11 | 市場ソートcomparatorバグ修正・ウォッチリスト市場バッジ統一 |
| 20260311m | 2026-03-11 | リファクタリング：makeTh/makePctCell共通化 |
| 20260311l | 2026-03-11 | 市場列・市場ソートを銘柄リスト/ウォッチリストに追加 |
| 20260311k | 2026-03-11 | ウォッチリストタブ実装 |
